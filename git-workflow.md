Workflow rules:
- main is the stable branch (deployable)
- no direct commits to main
- all work is done on a feature branch
- every feature branch is merged into main via a Pull Request
- each pull request must be reviewed by at least 1 teammate before merging
- use squash and merge to keep history clean

Commit messages:
- present tense
- short but descriptive names
